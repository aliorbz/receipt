# Deployment Scripts

Deployment is intentionally out of scope for Phase 3A.

Future Bradbury deployment scripts should live here and must read private keys only from a developer's local environment or wallet tooling. Do not commit private keys, seed phrases, generated key files, or deployment artifacts containing secrets.
